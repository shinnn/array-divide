{
  "name": "array-divide",
  "version": "2.0.0",
  "description": "Divide an array into multiple arrays each of whom have the same number of items",
  "repository": "shinnn/array-divide",
  "author": "Shinnosuke Watanabe (https://github.com/shinnn)",
  "scripts": {
    "pretest": "eslint --config @shinnn --ignore-path .gitignore . && bower install",
    "test": "node --strong_mode --throw-deprecation --track-heap-objects test.js | tap-spec",
    "coverage": "node --strong_mode node_modules/.bin/istanbul cover -x=bower_components/** test.js"
  },
  "license": "MIT",
  "files": [
    "index.js"
  ],
  "keywords": [
    "array",
    "divide",
    "separate",
    "slice",
    "quotition",
    "browser",
    "client-side"
  ],
  "dependencies": {
    "is-natural-number": "^4.0.1"
  },
  "devDependencies": {
    "@shinnn/eslint-config": "^2.0.0",
    "bower": "^1.7.7",
    "eslint": "^2.4.0",
    "istanbul": "^0.4.2",
    "require-bower-files": "^3.0.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.5.1"
  }
}
